---
vagrant_version: '1.9.4'
vagrant_manage_sudoers: false

# Download URL
vagrant_download_url: 'https://releases.hashicorp.com/vagrant/{{ vagrant_version }}'
vagrant_checksum_url: '{{ vagrant_download_url }}/vagrant_{{ vagrant_version }}_SHA256SUMS'

# Vagrant plugins
vagrant_plugin_users: []
vagrant_plugins: []
vagrant_plugins_keep_updated: false
